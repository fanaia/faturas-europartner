# Variáveis de infraestrutura — provisionadas automaticamente na publicação/ativação.
# O consultor não precisa editar este arquivo em produção.

SERVICE_NAME=faturas-europartner
SERVICE_VERSION=0.1.0
PORT=4000
NODE_ENV=development
MONGO_URI=mongodb://localhost:27017/faturas-europartner

CENTRAL_ATIVACAO_URL=https://central-ativacao.central.oondemand.online
CENTRAL_ATIVACAO_API_URL=https://central-ativacao.central.oondemand.online/api/
APP_CODE=faturas-europartner
APP_ENVIRONMENT=desenvolvimento
PUBLIC_APP_URL=http://localhost:5173
AUTH_PROVIDER_TIMEOUT_MS=10000
DEV_TOKEN=dev-local
INSTANCE_ID=local-dev

# Credenciais Omie, token de webhook, SendGrid, URLs de integração,
# timeouts, limites e parâmetros do worker são cadastrados na página
# Configurações da Central após a ativação.
